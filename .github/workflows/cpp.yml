name: C++ SDK

on:
  push:
    branches: [main, dev]
    paths: ["sdk/cpp/**", "cmd/**", "internal/**", "go.mod", "go.sum"]
  pull_request:
    branches: [main, dev]
    paths: ["sdk/cpp/**", "cmd/**", "internal/**", "go.mod", "go.sum"]

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            cc: gcc
            cxx: g++
          - os: ubuntu-latest
            cc: clang
            cxx: clang++
            sanitize: true
          - os: macos-latest
            cc: clang
            cxx: clang++
          # No self-pair integration test here (tests/CMakeLists.txt excludes it
          # on WIN32 - it's written directly against POSIX process/socket APIs).
          # The library and its unit test suite (relayly-tests) do build and run.
          - os: windows-latest
            cc: cl
            cxx: cl

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4

      # The self-pair integration test builds and runs the real cmd/relayly server.
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install C++ build prerequisites (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential libssl-dev

      # Puts cl.exe/link.exe on PATH the way a Developer Command Prompt would -
      # windows-latest runners don't have MSVC on PATH by default outside one.
      - name: Set up MSVC environment (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      # catch_discover_tests re-execs the same freshly-built, unsigned
      # relayly-tests.exe once per Catch2 test case (~20 invocations). Windows
      # Defender's real-time scanner is a well-documented GitHub Actions
      # windows-latest stall for exactly this pattern - observed hands-on here,
      # two separate hangs (13+ minutes, then a 120s timeout) on two *different*
      # otherwise-instant tests, both times one that writes/renames a file under
      # std::filesystem::temp_directory_path() (test_crypto.cpp/test_peerstore.cpp's
      # TempKeyPath()/TempStorePath() helpers) - a scan handle on a freshly-written
      # file can block a subsequent rename/reopen of it until the scan finishes.
      # Excluding paths one at a time chases whichever directory a given test
      # happens to touch; disabling real-time scanning entirely for this ephemeral
      # CI job (not a persistent, security-sensitive machine) is the complete fix.
      - name: Disable Windows Defender real-time monitoring (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: Set-MpPreference -DisableRealtimeMonitoring $true

      - name: Configure
        working-directory: sdk/cpp
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: |
          if [ "${{ matrix.sanitize }}" = "true" ]; then
            SANITIZE_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer"
          else
            SANITIZE_FLAGS=""
          fi
          if [ "${{ runner.os }}" = "Windows" ]; then
            # Ninja instead of the default multi-config Visual Studio generator so
            # CMAKE_BUILD_TYPE is honored the same way it is on Linux/macOS's
            # single-config generators, and ctest needs no --config to find binaries.
            GENERATOR_FLAGS="-G Ninja"
          else
            GENERATOR_FLAGS=""
          fi
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug $GENERATOR_FLAGS \
            -DCMAKE_C_FLAGS="$SANITIZE_FLAGS" \
            -DCMAKE_CXX_FLAGS="$SANITIZE_FLAGS" \
            -DCMAKE_EXE_LINKER_FLAGS="$SANITIZE_FLAGS"

      - name: Build
        working-directory: sdk/cpp
        run: cmake --build build -j 4

      # --timeout is a safety net, not the fix for the Defender stall above: a
      # genuinely hung test (this or a future one) should fail the job in minutes,
      # not exhaust the whole run's time budget silently.
      - name: Test
        working-directory: sdk/cpp
        run: ctest --test-dir build --output-on-failure --timeout 120
